# Copy to .env.local (or set in Vercel). See FOUNDER-TODO.md at repo root.
NEXT_PUBLIC_TALLY_FORM_URL=
# GA4 is NOT configured here. The measurement ID is a public client-side
# identifier, so it lives in src/config/site.ts as GA4_MEASUREMENT_ID, where a
# host allowlist keeps it from firing on localhost and preview deploys.
# This variable was never read by any code and is retired (2026-07-28).
