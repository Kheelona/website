# Secrets: never commit
.env
.env.*
!.env.example

# Dependencies / builds
node_modules/
.next/
out/
launch-video/out/

# OS noise
.DS_Store

# Compiled tool binaries
tools/cutout/cutout

# Raw photoshoot originals: stay on disk, never in git (620MB)
Design/product-images/Kheelona photoshoot 2025/

# QA screenshots and process artifacts
docs/snapshots/
3d-handoff/exports/
design-concepts/round-2-immersive/previews/

# Agent skill packages (2026-07-28 cleanup). These are tooling, not the website,
# and every one is reinstallable from skills-lock.json (which IS tracked, with a
# source and hash per skill). Keeping ~3MB of vendored markdown out of the repo.
.agents/skills/
.claude/skills/
.codex/skills/

# The founder brief exists here as a PDF (tracked, because docs reference it by
# name). The .docx is the same content in editable form — no need for both.
*.docx

# Next.js / tooling (merged up from site/.gitignore when the app moved to the
# repo root, 2026-07-28)
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions
/coverage
/storybook-static
/build
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.vercel
*.tsbuildinfo
next-env.d.ts

# Team feedback round 2026-07-30: committed as docs/revamp-2026-07/team-feedback-2026-07-30.pdf;
# the root originals (PDF + DOCX the founder dropped in) stay untracked working files.
/Website prompt based on B + inputs - 24-Jul.*
